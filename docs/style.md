## camelCase for "private" items

Use camelCase for variables, types, functions etc when they are not meant to be publicly exported, and use TitleCase for when they are meant to be public.

## File and folder names are camelCase

Applies to all new files and folders that aren't auto generated or named specifically by another library, package, script etc.
