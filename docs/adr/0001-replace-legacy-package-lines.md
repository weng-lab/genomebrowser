# Replace the legacy package lines

The implementations developed as v2 replace the legacy package lines under the canonical `@weng-lab/genomebrowser` and `@weng-lab/genomebrowser-ui` names. The repository keeps only `packages/core` and `packages/ui`; legacy implementations remain recoverable through Git history rather than an in-tree copy.
