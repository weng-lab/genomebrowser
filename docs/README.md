# Maintainer Docs

This folder contains repository-maintainer documentation: ADRs, design notes, style guidance, implementation concepts, and other contributor-facing material.

User-facing package documentation belongs in `packages/*/docs/` so it can ship with the package and be readable from `node_modules`.

Do not put package-user docs here unless they are only useful to maintainers of this repository.
